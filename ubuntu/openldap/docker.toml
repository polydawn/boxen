[default]
	image = "docket.polydawn.net/public/openldap"

	command = [ "/cwd/run.sh" ]

	mounts = [
		[ "./config/system", "/usr/share/slapd", "rw" ],
		[ "./config/schema", "/etc/ldap/schema", "rw" ],
		[ "./config/users",  "/service/users",   "rw" ],
	]

	ports = [
		[ "389", "389" ]
	]

[build]
	image = "docket.polydawn.net/public/ubuntu/12.04"

	command = [ "/cwd/build.sh" ]

[bash]
	command = [ "/bin/bash" ]
